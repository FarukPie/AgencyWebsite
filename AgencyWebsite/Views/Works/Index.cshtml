@model List<Work>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Layout/Index.cshtml";
}

<div class="row">

    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h3 class="card-title">Yapılan İşler Bölümü</h3>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Başlık</th>
                                <th>Sayı</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Title</td>
                                    <td>@item.Count</td>
                                    <td><a href="/Works/DeleteWork/@item.WorkId" class="btn btn-outline-danger">Sil</a></td>
                                    <td><a href="/Works/UpdateWork/@item.WorkId" class="btn btn-outline-success">Güncelle</a></td>
                                </tr>

                            }

                        </tbody>
                    </table>
                    <br><br />
                    <a href="/Works/CreateWork" class="btn btn-outline-primary">Yeni İş Ekle</a>
                </div>
            </div>
        </div>
    </div>
</div>
